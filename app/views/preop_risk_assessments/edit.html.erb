<h1>Editing preop_risk_assessment</h1>

<%= render 'form' %>

<%= link_to 'Back',
      patient_surgical_profile_path(
        @surgical_profile.patient_id, @surgical_profile.id,
        :sp_view_mode => SurgicalProfilesController::SP_VIEW_PREOP_ASSESSMENT
        )
%>
